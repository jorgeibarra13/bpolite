- [DONE] The popup.js file is probably not needed anymore. Maybe we should delete it
- Display the processed message in a textbox. ie I type in a mail a body, select, right click and it shows a suggestion after the text, if I click enter on my keyboard it swaps the current text with the processed one.
- Put this in a server somewhere, and expose an API.
  - create an api that does the calls instead
    - make the api handle the secret through a key vault or some other secure secret storage 
  - make the extension call that API instead